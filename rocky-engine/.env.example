# Rocky engine local configuration.
#
# Copy this file to `.env` and fill only the integrations you want to use.
# Never commit the real `.env`.

# Required for Llama/Whisper via Groq. Without it Rocky uses fallback responses.
GROQ_API_KEY=

# Optional: local models via Ollama. Rocky only accepts a loopback URL.
ROCKY_MODEL_PROVIDER=groq
ROCKY_OLLAMA_MODEL=
OLLAMA_HOST=http://127.0.0.1:11434
ROCKY_WEB_ENABLED=true
# Read-only workspace searched by `workspace.search`; defaults to the repo root.
ROCKY_WORKSPACE_ROOT=

# Capability policy. System telemetry is always read-only; disable optional
# integrations explicitly when the agent must not access them.
ROCKY_ALLOW_WORKSPACE_READ=true
ROCKY_ALLOW_CALENDAR_READ=true
ROCKY_ALLOW_SPOTIFY_CONTROL=true

# Optional: structured log verbosity.
ROCKY_LOG_LEVEL=INFO

# Optional: Spotify tools.
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=
SPOTIFY_REDIRECT_URI=http://127.0.0.1:8888/callback

# Optional: Google Calendar tools.
# Point to a Google service-account or OAuth user JSON file.
GOOGLE_APPLICATION_CREDENTIALS=
